[package]
name = "deepseek_client"
version = "0.1.0"
edition = "2021"

[dependencies]
serde_json = "1"
ureq = { version = "2", features = ["json"] }

[dev-dependencies]
tokio = { version = "1", features = ["rt", "macros"] }
wiremock = "0.5"
