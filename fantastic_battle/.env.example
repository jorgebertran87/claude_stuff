# DeepSeek API key — enables AI-generated NPC names and trivia questions.
# When unset, the game falls back to stub content (hardcoded names and questions).
DEEPSEEK_API_KEY=

# Server port (default: 8080)
PORT=8080

# Log level (default: info)
RUST_LOG=info
