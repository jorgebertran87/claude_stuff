services:
  integration-tests:
    image: voice-assistant-rust-test
    network_mode: host
    environment:
      - SDL_AUDIODRIVER=dummy
      - SDL_VIDEODRIVER=dummy
    volumes:
      - ${HOME}/.claude:/root/.claude
