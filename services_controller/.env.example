# Path to the YAML file declaring alias → service mappings.
ALIAS_CONFIG=/config/aliases.yaml

# Docker Engine HTTP API endpoint (a TCP proxy in front of the daemon socket).
DOCKER_API_URL=http://localhost:2375
